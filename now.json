{
  "version": 2,
  "name": "thai-lotto",
  "builds": [{
    "src": "/src/index.ts",
    "use": "@now/node-server",
    "config": { "maxLambdaSize": "50mb" }
  }],
  "routes": [{
    "src": "/(.*)",
    "dest": "/src/index.ts"
  }],
  "regions": ["all"]
}
